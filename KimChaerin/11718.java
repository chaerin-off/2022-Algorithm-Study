import java.util.*;

public class Main {
    public static void main(String[] args) {
    	Scanner sc = new Scanner(System.in);
    	while(sc.hasNext()) {
    		String s = sc.nextLine(); //띄어쓰기를 포함한 한 줄의 문자열은 nextLine()으로 입력받는다.
    		System.out.println(s);
    	}
    }
}
